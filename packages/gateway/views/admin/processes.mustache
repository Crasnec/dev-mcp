<section class="panel">
  <form class="toolbar" method="get" action="/admin/processes">{{> partials/sort-fields}}{{> partials/owner-filter}}<label class="filter-field grow">명령 검색<input type="search" name="q" value="{{q}}" placeholder="실행 명령"></label><label class="filter-field">상태<select name="status"><option value="">전체 상태</option>{{#statuses}}<option value="{{value}}" {{#selected}}selected{{/selected}}>{{label}}</option>{{/statuses}}</select></label><button class="primary" type="submit">조회</button><a class="button quiet" href="/admin/processes?owner={{owner.id}}">초기화</a></form>
  {{> partials/runner-unavailable}}
  <div class="table-scroll"><table><caption class="sr-only">프로세스 목록</caption><thead><tr>{{#sortHeaders.command}}{{> partials/sortable-header}}{{/sortHeaders.command}}{{#sortHeaders.status}}{{> partials/sortable-header}}{{/sortHeaders.status}}{{#sortHeaders.pid}}{{> partials/sortable-header}}{{/sortHeaders.pid}}{{#sortHeaders.started}}{{> partials/sortable-header}}{{/sortHeaders.started}}<th>관리</th></tr></thead><tbody>
    {{#rows}}<tr><td><code class="command-preview">{{command}}</code></td><td><span class="badge {{status}}">{{statusLabel}}</span></td><td>{{pid}}</td><td>{{startedLabel}}</td><td><a class="button small" href="{{href}}">로그·상세 →</a></td></tr>{{/rows}}
    {{^rows}}<tr><td colspan="5" class="empty-state">표시할 프로세스가 없습니다.</td></tr>{{/rows}}
  </tbody></table></div>{{> partials/pagination}}
</section>
